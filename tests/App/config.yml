parameters:
    log_file: %kernel.logs_dir%/%kernel.environment%.log
    secret: ThisTokenIsNotSoSecretChangeIt

framework:
    test: ~
    secret: %secret%

services:
    dummy_command:
        class: tests\ClearcodeHQ\CommandBusLauncherBundle\Mocks\DummyCommandHandler
        tags:
            - { name: command_handler, handles: tests\ClearcodeHQ\CommandBusLauncherBundle\Mocks\DummyCommand }