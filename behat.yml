default:
    autoload:
        - %paths.base%
    suites:
        default:
            filters:
                tags: ~@incomplete
            contexts:
                - \tests\Clearcode\CommandBusConsole\Context\CommandContext
